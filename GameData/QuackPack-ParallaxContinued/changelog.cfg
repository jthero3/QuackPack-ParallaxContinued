// Changelog file generated by yaclog-ksp (https://git.offworldcolonies.nexus/drewcassidy/yaclog-ksp)
KERBALCHANGELOG
{
    modName = QuackPack-ParallaxContinued
    VERSION {
        version = 0.5.2
        versionDate = 2026-08-03
        change = There is nothing permanent except change
        CHANGE {
            type = Added
            change = Updated Pipelines with KSPBuildTools
        }

        CHANGE {
            type = Added
            change = Fixed gloss map portion of the terrain textures.
        }

        CHANGE {
            type = Added
            change = Updated asset bundles.
        }

    }

    VERSION {
        version = [0.5.0]
        CHANGE {
            type = Added
            change = Added a version file to install.
        }

        CHANGE {
            type = Changed
            change =  All textures have been moved into asset bundles. Reducing file sizes by over half.
        }

    }

    VERSION {
        version = [0.3.4]
        CHANGE {
            type = Fixed
            change = Re-added materials node required for map previews.
        }

        CHANGE {
            type = Added
            change = Created Release Pipeline.
        }

    }

    VERSION {
        version = [0.3.3]
        CHANGE {
            type = Fixed
            change = VertexColorMapEmissive Fix: Removed Mod from Ocean, and Scaled Sections.
        }

    }

    VERSION {
        version = [0.3.2]
        CHANGE {
            type = Changed
            change = Updated Atmo Configs
        }

    }

    VERSION {
        version = [0.3.1]
        CHANGE {
            type = Fixed
            change = Sigma Rescale Fix: Added compatibility patch for sigma rescales.
        }

    }

    VERSION {
        version = [0.3.0]
        CHANGE {
            type = Changed
            change = Re-did Geet Scatters.
        }

        CHANGE {
            type = Changed
            change = Cleaned up Scaled Space for Geet/Blas/Cind.
        }

        CHANGE {
            type = Changed
            change = Touch-Ups on Subon.
        }

    }

    VERSION {
        version = [0.2.0]
        CHANGE {
            type = Added
            change = Adds Full ScaledSpace Support for all bodies. Release 3 Required.
        }

    }

    VERSION {
        version = [0.1.0]
        CHANGE {
            type = Added
            change = Adds Full Texture and Scatter Support.
        }

    }

}
